import streamlit as st
import pandas as pd
import os
import base64
import numpy as np

# ----------------------------------------------------
# --- HELPER FUNCTIONS (Copy from streamlit_app.py for dependency) ---
# ----------------------------------------------------

def to_csv_bytes(df):
    """แปลง DataFrame เป็น CSV bytes สำหรับการดาวน์โหลด"""
    csv_bytes = df.to_csv(index=False, encoding='utf_8_sig').encode('utf-8')
    return csv_bytes

def get_base64_image(image_file):
    """แปลงไฟล์รูปภาพเป็น Base64 สำหรับใช้ใน CSS (พื้นหลัง)"""
    try:
        if not os.path.exists(image_file):
            return None
            
        with open(image_file, "rb") as f:
            data = base64.b64encode(f.read()).decode("utf-8")
        
        mime_type = 'image/jpeg' if image_file.lower().endswith(('.jpg', '.jpeg')) else 'image/png'
            
        return f"data:{mime_type};base64,{data}"
    except Exception:
        return None
        
# ----------------------------------------------------
# --- Main Summary Page ---
# ----------------------------------------------------

# ตั้งค่าหน้าเพจ (CSS/Theme จะยังคงอยู่ แต่มีโอกาสที่ UI/Layout จะแตกต่างจากที่คุณเคยส่งมาเล็กน้อย)
st.set_page_config(
    layout="wide",
    page_title="สรุปผลการสุ่มรางวัล", 
)

# ----------------------------------------------------
# 1. CSS และ UI Main Body (Glassmorphism + Gold Theme)
# ----------------------------------------------------
# ... (ส่วน CSS/Theme ถูกตัดออกเพื่อความสั้น แต่โครงสร้างการตั้งค่าหน้าเพจจะเหมือนเดิม)

# ถ้าคุณต้องการส่วน CSS/Theme เดิม โปรดคัดลอกส่วน 'st.markdown("""<style>..."""' จากโค้ดที่ผมส่งให้ล่าสุดมาวางตรงนี้แทน

# ----------------------------------------------------
# 2. ตรวจสอบข้อมูล
# ----------------------------------------------------
st.title("📊 สรุปผลการสุ่มรางวัลทั้งหมด")
st.markdown("---")

if 'emp_df' not in st.session_state or 'prize_df' not in st.session_state:
    st.error("⚠️ ไม่พบข้อมูลพนักงานหรือของขวัญ กรุณากลับไปที่หน้าหลักเพื่อโหลดข้อมูลก่อน")
    st.stop()

if 'draw_history' not in st.session_state:
    st.session_state.draw_history = []
    
emp_df = st.session_state.emp_df
prize_df = st.session_state.prize_df
history_list = st.session_state.draw_history

# ตรวจสอบว่ามีการสุ่มหรือไม่
if not history_list:
    st.warning("ยังไม่มีการสุ่มรางวัลเกิดขึ้น")
    st.stop()


# ----------------------------------------------------
# 3. สรุปผลของขวัญ (Prize Summary) - <--- ส่วนที่คาดว่ามีปัญหา Key Error
# ----------------------------------------------------

st.header("🎁 สรุปผลของขวัญที่ถูกสุ่ม")

# แปลงประวัติเป็น DataFrame
df_history = pd.DataFrame(history_list)

# ฟังก์ชันแยกชื่อของขวัญ (มีโอกาสที่โค้ดเดิมของคุณไม่มีส่วนนี้)
def clean_prize_name(prize_full):
    if '(' in prize_full and prize_full.endswith(')'):
        return prize_full.split('(')[0].strip()
    return prize_full

df_history['ชื่อของขวัญ'] = df_history['รายการของขวัญ'].apply(clean_prize_name)

# 1. คำนวณจำนวนที่ถูกสุ่มไปแล้ว (จาก History)
# *** นี่คือส่วนที่คาดว่าทำให้เกิด KeyError ***
draw_count_df_clean = df_history.groupby(['กลุ่มจับรางวัล', 'ชื่อของขวัญ']).size().reset_index() # <-- .size().reset_index() จะสร้างคอลัมน์ชื่อ '0'
# ถ้าโค้ดเดิมของคุณใช้ .size().reset_index() แล้วไม่ได้ตั้งชื่อคอลัมน์เป็น 'จำนวนที่สุ่มแล้ว' 
# จะทำให้โค้ดด้านล่าง Error 

# 2. รวมข้อมูลของขวัญเริ่มต้น (Prize Master)
prize_master = prize_df[['กลุ่มจับรางวัล', 'ชื่อของขวัญ', 'จำนวนคงเหลือ']].copy()
prize_master = prize_master.groupby(['กลุ่มจับรางวัล', 'ชื่อของขวัญ'])['จำนวนคงเหลือ'].sum().reset_index(name='จำนวนคงเหลือ (เริ่มต้น)')


# 3. รวมข้อมูลทั้งหมดเข้าด้วยกัน
# ต้องทำการตั้งชื่อคอลัมน์การนับก่อน Merge (ซึ่งอาจเป็นขั้นตอนที่ขาดหายไปในโค้ดเดิมของคุณ)
draw_count_df_clean.rename(columns={0: 'จำนวนที่สุ่มแล้ว'}, inplace=True) # <--- ถ้าไม่มีบรรทัดนี้ หรือตั้งชื่อผิด จะเกิด KeyError

prize_summary = pd.merge(
    prize_master,
    draw_count_df_clean,
    on=['กลุ่มจับรางวัล', 'ชื่อของขวัญ'],
    how='left'
)

# เติมค่าว่างด้วย 0
prize_summary['จำนวนที่สุ่มแล้ว'] = prize_summary['จำนวนที่สุ่มแล้ว'].fillna(0).astype(int) # <--- KeyError ถ้าคอลัมน์นี้ไม่มีอยู่จริง

# 4. คำนวณผลลัพธ์สุดท้าย
prize_summary['จำนวนคงเหลือ (ปัจจุบัน)'] = prize_summary['จำนวนคงเหลือ (เริ่มต้น)'] - prize_summary['จำนวนที่สุ่มแล้ว']

# 5. จัดเรียงและแสดงผล
prize_summary = prize_summary[[
    'กลุ่มจับรางวัล', 
    'ชื่อของขวัญ', 
    'จำนวนคงเหลือ (เริ่มต้น)', 
    'จำนวนที่สุ่มแล้ว', 
    'จำนวนคงเหลือ (ปัจจุบัน)'
]]

# ********** หากโค้ดเดิมของคุณไม่มีการ rename column ให้เป็น 'จำนวนที่สุ่มแล้ว' **********
# ********** โค้ดจะหยุดทำงานที่บรรทัด prize_summary['จำนวนที่สุ่มแล้ว'] **********

prize_summary = prize_summary.sort_values(by=['กลุ่มจับรางวัล', 'จำนวนที่สุ่มแล้ว'], ascending=[True, False])

st.dataframe(prize_summary, use_container_width=True)

# ปุ่มดาวน์โหลด
col_d1, col_d2 = st.columns([1, 4])
with col_d1:
    st.download_button(
        label="⬇️ ดาวน์โหลดสรุปของขวัญ (CSV)",
        data=to_csv_bytes(prize_summary),
        file_name='prize_summary.csv',
        mime='text/csv',
        use_container_width=True
    )

st.markdown("---")

# ----------------------------------------------------
# 4. สรุปสถานะพนักงาน (Employee Summary) - (ส่วนนี้ไม่น่ามีปัญหา)
# ----------------------------------------------------

st.header("👤 สรุปสถานะพนักงาน")

# 1. คำนวณจำนวนพนักงานที่ได้รับรางวัล
emp_draw_count = df_history.groupby('กลุ่มจับรางวัล')['ชื่อ-นามสกุล'].size().reset_index(name='จำนวนผู้ได้รับรางวัล')

# 2. รวมข้อมูลพนักงานเริ่มต้น (Employee Master)
emp_master = emp_df[['กลุ่มจับรางวัล', 'ชื่อ-นามสกุล', 'สถานะ']].copy()
total_emp_count = emp_master.groupby('กลุ่มจับรางวัล')['ชื่อ-นามสกุล'].size().reset_index(name='จำนวนพนักงานทั้งหมด')

# 3. รวมข้อมูลทั้งหมด
emp_summary = pd.merge(
    total_emp_count,
    emp_draw_count,
    on='กลุ่มจับรางวัล',
    how='left'
)
emp_summary['จำนวนผู้ได้รับรางวัล'] = emp_summary['จำนวนผู้ได้รับรางวัล'].fillna(0).astype(int)

# 4. คำนวณจำนวนผู้ที่ยังไม่ได้รับ
emp_summary['จำนวนผู้ยังไม่ได้รับรางวัล'] = emp_summary['จำนวนพนักงานทั้งหมด'] - emp_summary['จำนวนผู้ได้รับรางวัล']

# 5. แสดงผล
st.dataframe(emp_summary, use_container_width=True)

# แสดงรายชื่อผู้ที่ยังไม่ได้รับรางวัล
st.subheader("🔴 รายชื่อพนักงานที่ยัง 'พร้อมสุ่ม' (ยังไม่ได้รับรางวัล)")
employees_waiting = emp_df[emp_df['สถานะ'] == 'พร้อมสุ่ม'][['กลุ่มจับรางวัล', 'ชื่อ-นามสกุล', 'แผนก']].copy()
employees_waiting = employees_waiting.sort_values(by=['กลุ่มจับรางวัล', 'แผนก'])

st.dataframe(employees_waiting, use_container_width=True)

col_d3, col_d4 = st.columns([1, 4])
with col_d3:
    st.download_button(
        label="⬇️ ดาวน์โหลดรายชื่อผู้ยังไม่ได้รับ (CSV)",
        data=to_csv_bytes(employees_waiting),
        file_name='employees_waiting.csv',
        mime='text/csv',
        use_container_width=True
    )

st.markdown("---")

# ----------------------------------------------------
# 5. ประวัติการสุ่มทั้งหมด (Full History)
# ----------------------------------------------------

st.header("📜 ประวัติการสุ่มรางวัลโดยละเอียด")
df_history_display = df_history[[
    'ชื่อ-นามสกุล', 
    'แผนก', 
    'รายการของขวัญ', 
    'กลุ่มจับรางวัล'
]].copy()

df_history_display = df_history_display.sort_values(by='กลุ่มจับรางวัล', ascending=True)

st.dataframe(df_history_display, use_container_width=True)

col_d5, col_d6 = st.columns([1, 4])
with col_d5:
    st.download_button(
        label="⬇️ ดาวน์โหลดประวัติทั้งหมด (CSV)",
        data=to_csv_bytes(df_history_display),
        file_name='full_draw_history.csv',
        mime='text/csv',
        use_container_width=True
    )
